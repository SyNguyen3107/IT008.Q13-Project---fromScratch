<Window x:Class="EasyFlips.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Room Settings" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="Transparent" 
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <SolidColorBrush x:Key="BgDark" Color="#181A20"/>
        <SolidColorBrush x:Key="PanelDark" Color="#23262F"/>
        <SolidColorBrush x:Key="PrimaryPurple" Color="#6949ff"/>
        <SolidColorBrush x:Key="TextGrey" Color="#9E9EA7"/>
        <SolidColorBrush x:Key="AccentCyan" Color="#00D4FF"/>

        <Style x:Key="NeonSlider" TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border Name="TrackBackground" Background="#333" Height="4" CornerRadius="2" VerticalAlignment="Center"/>
                            <Track Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Background="{StaticResource PrimaryPurple}" Height="4">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="2" Height="4"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Grid>
                                                    <Ellipse Width="20" Height="20" Fill="White">
                                                        <Ellipse.Effect>
                                                            <DropShadowEffect Color="#6C5DD3" BlurRadius="10" Opacity="0.5" ShadowDepth="0"/>
                                                        </Ellipse.Effect>
                                                    </Ellipse>
                                                    <Ellipse Width="10" Height="10" Fill="{StaticResource PrimaryPurple}"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextGrey}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="ValueText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BgDark}" CornerRadius="20" BorderBrush="#333" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="30" Opacity="0.6"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Padding="30,25,30,10" BorderBrush="#2B2E38" BorderThickness="0,0,0,1">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚙" Foreground="{StaticResource PrimaryPurple}" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="ROOM SETTINGS" Foreground="White" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Content="✕" Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                            HorizontalAlignment="Right" Background="Transparent" Foreground="#666" BorderThickness="0" FontSize="16" Cursor="Hand"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="240"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,30,0">
                    <TextBlock Text="SELECTED DECK" Style="{StaticResource HeaderLabel}"/>

                    <Border Background="{StaticResource PanelDark}" CornerRadius="15" Height="220" Cursor="Hand">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.3"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="🎴" FontSize="80" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.1" Foreground="White"/>

                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Choose Topic:" Foreground="#666" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                <ComboBox ItemsSource="{Binding AvailableDecks}"
                                          SelectedItem="{Binding SelectedDeck}"
                                          DisplayMemberPath="Name"
                                          Height="40" Width="180"
                                          FontSize="14" FontWeight="Bold"
                                          HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                                          Background="Transparent" BorderBrush="{StaticResource PrimaryPurple}" BorderThickness="0,0,0,2"/>
                            </StackPanel>

                            <Border Grid.Row="1" Background="{StaticResource PrimaryPurple}" CornerRadius="0,0,15,15" Padding="15">
                                <TextBlock Text="{Binding SelectedDeck.Name, FallbackValue='No Deck Selected'}" 
                                           Foreground="White" FontWeight="Bold" TextAlignment="Center" TextWrapping="Wrap"/>
                            </Border>
                        </Grid>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,0,-29">
                    <TextBlock Text="GAME PARAMETERS" Style="{StaticResource HeaderLabel}" Margin="0,0,0,20"/>

                    <Border Background="{StaticResource PanelDark}" CornerRadius="12" Padding="15" Margin="0,0,0,15">
                        <StackPanel>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <TextBlock Text="👥" Foreground="#ffc107" Margin="0,0,8,0" FontSize="16"/>
                                    <TextBlock Text="Max Players" VerticalAlignment="Center" Foreground="#CCC"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" Text="{Binding MaxPlayers, StringFormat='{}{0}'}" 
                                           Style="{StaticResource ValueText}" Foreground="{StaticResource AccentCyan}"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                            <Slider Style="{StaticResource NeonSlider}" Minimum="2" Maximum="50"
                                    Value="{Binding MaxPlayersDouble, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    TickFrequency="1" IsSnapToTickEnabled="True"/>
                        </StackPanel>
                    </Border>

                    <Border Background="{StaticResource PanelDark}" CornerRadius="12" Padding="15" Margin="0,0,0,15">
                        <StackPanel>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <TextBlock Text="⏱" Foreground="#ffc107" Margin="0,0,8,0" FontSize="16"/>
                                    <TextBlock Text="Time per Round" VerticalAlignment="Center" Foreground="#CCC"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" Text="{Binding TimePerRound, StringFormat='{}{0}s'}" 
                                           Style="{StaticResource ValueText}" Foreground="#FFB039"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                            <Slider Style="{StaticResource NeonSlider}" Minimum="5" Maximum="60" 
                                    Value="{Binding TimePerRound}" 
                                    TickFrequency="5" IsSnapToTickEnabled="True"/>
                        </StackPanel>
                    </Border>

                    <Border Background="{StaticResource PanelDark}" CornerRadius="12" Padding="15">
                        <StackPanel>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <TextBlock Text="⏳" Foreground="#ffc107" Margin="0,0,8,0" FontSize="16"/>
                                    <TextBlock Text="Auto-start Timer" VerticalAlignment="Center" Foreground="#CCC"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" HorizontalAlignment="Right">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource ValueText}">
                                            <Setter Property="Text" Value="{Binding WaitTimeMinutes, StringFormat='{}{0} min'}"/>
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryPurple}"/>

                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding WaitTimeMinutes}" Value="0">
                                                    <Setter Property="Text" Value="Manual"/>
                                                    <Setter Property="Foreground" Value="#FF5E57"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                            <Slider Style="{StaticResource NeonSlider}" Minimum="0" Maximum="15" 
                                    Value="{Binding WaitTimeMinutes}" 
                                    TickFrequency="1" IsSnapToTickEnabled="True"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="2" Margin="30,0,30,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="CANCEL" FontSize="14" Height="50"
                        Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Foreground="White" FontWeight="Bold" Cursor="Hand" BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bdr" Background="#2B2E38" CornerRadius="15">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bdr" Property="Background" Value="#353945"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Grid.Column="2" Content="SAVE SETTINGS" FontSize="14" Height="50"
                        Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Foreground="White" FontWeight="Bold" Cursor="Hand" BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bdr" Background="{StaticResource PrimaryPurple}" CornerRadius="15">
                                <Border.Effect>
                                    <DropShadowEffect Color="#6C5DD3" BlurRadius="15" Opacity="0.4" ShadowDepth="0"/>
                                </Border.Effect>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>