<Window x:Class="EasyFlips.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Room Settings" Height="550" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="#1A2232" ResizeMode="NoResize">

    <Window.Resources>
        <!-- Label style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,15,0,5"/>
        </Style>

        <!-- Value style -->
        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
    </Window.Resources>

    <Border Padding="30">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Nội dung chính -->
            <ScrollViewer Grid.Row="0" 
                          VerticalScrollBarVisibility="Disabled" 
                          HorizontalScrollBarVisibility="Disabled" 
                          Focusable="False">
                <StackPanel>
                    <!-- Title -->
                    <TextBlock Text="ROOM SETTINGS" Foreground="White" FontSize="24" FontWeight="Bold"
                               HorizontalAlignment="Center" Margin="0,0,0,30">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#3B82F6" BlurRadius="15" Opacity="0.4" ShadowDepth="0"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <!-- Deck -->
                    <TextBlock Text="Select Deck" Style="{StaticResource LabelStyle}"/>
                    <ComboBox ItemsSource="{Binding AvailableDecks}"
                              SelectedItem="{Binding SelectedDeck}"
                              DisplayMemberPath="Name"
                              Height="40" FontSize="14" Padding="10,0"
                              Background="#2D3748" Foreground="Black" BorderThickness="0"/>

                    <!-- Max Players -->
                    <Grid Margin="0,20,0,0">
                        <TextBlock Text="Max Players" Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding MaxPlayers, StringFormat='{}{0} players'}" Style="{StaticResource ValueStyle}"/>
                    </Grid>
                    <Slider Minimum="2" Maximum="50"
                            Value="{Binding MaxPlayersDouble, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,5,0,0"/>

                    <!-- Time Per Round -->
                    <Grid Margin="0,15,0,0">
                        <TextBlock Text="Time Per Round" Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding TimePerRound, StringFormat='{}{0} seconds'}" Style="{StaticResource ValueStyle}"/>
                    </Grid>
                    <Slider Minimum="5" Maximum="60" Value="{Binding TimePerRound}" TickFrequency="5" Margin="0,5,0,0"/>

                    <!-- Wait Time -->
                    <Grid Margin="0,15,0,0">
                        <TextBlock Text="Auto-start after" Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
                        <TextBlock>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource ValueStyle}">
                                    <Setter Property="Text" Value="{Binding WaitTimeMinutes, StringFormat='{}{0} minutes'}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding WaitTimeMinutes}" Value="0">
                                            <Setter Property="Text" Value="Manual Start"/>
                                            <Setter Property="Foreground" Value="#F59E0B"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                    <Slider Minimum="0" Maximum="15" Value="{Binding WaitTimeMinutes}" TickFrequency="1" Margin="0,5,0,0"/>
                    <TextBlock Text="Set to 0 to disable auto-start" Foreground="#64748B" FontSize="11" FontStyle="Italic" Margin="0,2,0,0"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Buttons cố định dưới cùng -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="CANCEL" Width="120" Height="45" Margin="0,0,15,0"
                        Background="#64748B" Foreground="White" FontWeight="Bold" FontSize="14"
                        Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        BorderThickness="0" Cursor="Hand"/>
                <Button Content="SAVE" Width="120" Height="45"
                        Background="#3B82F6" Foreground="White" FontWeight="Bold" FontSize="14"
                        Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
