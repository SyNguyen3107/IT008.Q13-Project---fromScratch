<UserControl x:Class="EasyFlips.Views.StudyCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:EasyFlips.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="800"
             Background="#1E1E1E">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <converters:InvertedBooleanToVisibilityConverter x:Key="InvertedBoolToVisConverter"/>
        <converters:NullToCollapsedConverter x:Key="NullToCollapsedConverter"/>
        
            <converters:ImagePathConverter x:Key="ImagePathConverter"/>
        
        <converters:DiffTypeToBrushConverter x:Key="DiffTypeToBrushConverter"/>
        <converters:DiffTypeToTextDecorationConverter x:Key="DiffTypeToTextDecorationConverter"/>

        <Style x:Key="GradeButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" CornerRadius="20" Padding="25"
                Background="#2D2D30" 
                BorderThickness="0,4,0,0" 
                BorderBrush="#6C5DD3">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.3" ShadowDepth="5"/>
            </Border.Effect>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel VerticalAlignment="Center">

                    <TextBlock Text="{Binding QuestionText}" 
                               Foreground="White" FontSize="32" FontWeight="SemiBold"
                               TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,15"/>

                    <Image Source="{Binding FrontImagePath, Converter={StaticResource ImagePathConverter}}"
                           Stretch="Uniform" MaxHeight="250" HorizontalAlignment="Center" Margin="0,0,0,15"
                           Visibility="{Binding FrontImagePath, Converter={StaticResource NullToCollapsedConverter}}"/>

                    <Button Command="{Binding PlayAudioCommand}" CommandParameter="{Binding FrontAudioPath}"
                            Visibility="{Binding FrontAudioPath, Converter={StaticResource NullToCollapsedConverter}}"
                            HorizontalAlignment="Center" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,0,10">
                        <TextBlock Text="🔊 Play Question Audio" Foreground="#A29BFE" FontSize="16"/>
                    </Button>

                    <StackPanel Visibility="{Binding IsAnswerVisible, Converter={StaticResource BoolToVisConverter}}">

                        <Separator Margin="0,20" Background="#444" Height="2"/>

                        <ItemsControl ItemsSource="{Binding ComparisonPieces}" HorizontalAlignment="Center" Margin="0,0,0,10">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Text}" FontSize="26" Margin="1,0"
                                               Foreground="{Binding Type, Converter={StaticResource DiffTypeToBrushConverter}}"
                                               TextDecorations="{Binding Type, Converter={StaticResource DiffTypeToTextDecorationConverter}}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                            <TextBlock Text="Correct: " Foreground="LightGray" FontSize="16"/>
                            <TextBlock Text="{Binding CorrectAnswer}" Foreground="#2ECC71" FontSize="22" FontWeight="Bold"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                            <TextBlock Text="Score: " Foreground="LightGray" FontSize="16"/>
                            <TextBlock Text="{Binding TotalScore}" Foreground="#2ECC71" FontSize="22" FontWeight="Bold"/>
                        </StackPanel>

                        <Image Source="{Binding BackImagePath, Converter={StaticResource ImagePathConverter}}"
                               Stretch="Uniform" MaxHeight="250" HorizontalAlignment="Center" Margin="0,0,0,15"
                               Visibility="{Binding BackImagePath, Converter={StaticResource NullToCollapsedConverter}}"/>

                        <Button Command="{Binding PlayAudioCommand}" CommandParameter="{Binding BackAudioPath}"
                                Visibility="{Binding BackAudioPath, Converter={StaticResource NullToCollapsedConverter}}"
                                HorizontalAlignment="Center" Background="Transparent" BorderThickness="0" Cursor="Hand">
                            <TextBlock Text="🔊 Play Answer Audio" Foreground="#A29BFE" FontSize="16"/>
                        </Button>

                        <TextBlock Text="{Binding AnswerText}" Foreground="#DDD" FontSize="18" 
                                   FontStyle="Italic" TextAlignment="Center" Margin="0,15"
                                   Visibility="{Binding AnswerText, Converter={StaticResource NullToCollapsedConverter}}"/>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="1" Margin="0,25,0,0" 
              Visibility="{Binding IsAnswerVisible, Converter={StaticResource InvertedBoolToVisConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Your Answer:" Foreground="#B2BEC3" VerticalAlignment="Center" Margin="0,0,15,0" FontSize="18"/>
            <TextBox Grid.Column="1" 
                 Text="{Binding UserInputText, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="20" 
                 Padding="2,2,2,5" 
                 Background="#2D2D30"
                 Foreground="White" 
                 BorderThickness="0,0,0,2" 
                 BorderBrush="#6C5DD3"
                 CaretBrush="White"
                 VerticalContentAlignment="Center">
                <TextBox.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="0"/>
                    </Style>
                </TextBox.Resources>
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding ShowAnswerCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
        </Grid>

        <Grid Grid.Row="2" Margin="0,30,0,0">
            <Button Content="SHOW ANSWER" Style="{StaticResource ModernButtonStyle}"
                    FontSize="18" FontWeight="Bold" Height="55"
                    Command="{Binding ShowAnswerCommand}"
                    Visibility="{Binding IsAnswerVisible, Converter={StaticResource InvertedBoolToVisConverter}}">
                <Button.Effect>
                    <DropShadowEffect BlurRadius="15" Color="{StaticResource Color.Secondary}" Opacity="0.5" ShadowDepth="0"/>
                </Button.Effect>
            </Button>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"
                        Visibility="{Binding IsAnswerVisible, Converter={StaticResource BoolToVisConverter}}">
                <Button Content="Again" Style="{StaticResource GradeButtonStyle}" Background="#E74C3C" Command="{Binding AgainCommand}"/>
                <Button Content="Hard" Style="{StaticResource GradeButtonStyle}" Background="#F39C12" Command="{Binding HardCommand}"/>
                <Button Content="Good" Style="{StaticResource GradeButtonStyle}" Background="#2ECC71" Command="{Binding GoodCommand}"/>
                <Button Content="Easy" Style="{StaticResource GradeButtonStyle}" Background="#3498DB" Command="{Binding EasyCommand}"/>
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>