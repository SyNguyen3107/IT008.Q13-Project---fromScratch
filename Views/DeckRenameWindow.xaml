<Window x:Class="EasyFlips.Views.DeckRenameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasyFlips.Views"
        mc:Ignorable="d"
        x:Name="ThisWindow"
        Title="Rename Deck" Height="200" Width="400"
        ResizeMode="NoResize"  
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <SolidColorBrush x:Key="SurfaceDark" Color="#1F222A"/>
        <SolidColorBrush x:Key="PrimaryPurple" Color="#6C5DD3"/>
        <SolidColorBrush x:Key="TextGrey" Color="#9E9EA7"/>
        <SolidColorBrush x:Key="InputBg" Color="#2B2E38"/>

        <Style x:Key="PrimaryBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryPurple}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="#2B2E38"/>
            <Setter Property="Foreground" Value="{StaticResource TextGrey}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="10" BorderBrush="#444" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="#353945"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource SurfaceDark}" CornerRadius="15" BorderBrush="#333" BorderThickness="1" Margin="0,0,0,-27">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="25,25,25,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Rename Deck" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <TextBlock Text="New Name" Foreground="{StaticResource TextGrey}" Margin="0,0,0,8" FontSize="13" FontWeight="SemiBold"/>
                <Border CornerRadius="8" Background="{StaticResource InputBg}" BorderBrush="#333" BorderThickness="1" Height="40">
                    <TextBox x:Name="NewNameTextBox"
                             Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}"
                             Background="Transparent" BorderThickness="0"
                             Foreground="White" FontSize="14"
                             VerticalContentAlignment="Center" Padding="10,0"/>
                </Border>
            </StackPanel>

            <Grid Grid.Row="3" Margin="0,25,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="CANCEL" Height="40"
                        Command="{Binding CancelCommand}" 
                        CommandParameter="{Binding ElementName=ThisWindow}"
                        Style="{StaticResource SecondaryBtnStyle}"/>

                <Button Grid.Column="2" Content="RENAME" Height="40"
                        Command="{Binding RenameCommand}" 
                        CommandParameter="{Binding ElementName=ThisWindow}"
                        Style="{StaticResource PrimaryBtnStyle}"/>
            </Grid>
        </Grid>
    </Border>
</Window>