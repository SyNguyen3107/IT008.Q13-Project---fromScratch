<Window x:Class="EasyFlips.Views.DeckChosenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        
        Title="{Binding DeckName}" 
        Height="300" Width="480" 
        ResizeMode="NoResize" 
        WindowStartupLocation="CenterScreen"
        Background="#2c2c2c">
    <!-- Lưu ý: Title đã được Bind vào DeckName -->

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề (Cũng hiện tên Deck ở đây cho đẹp) -->
        <TextBlock Grid.Row="0" Text="{Binding DeckName}" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,12" TextAlignment="Center"/>

        <!-- Các chỉ số -->
        <StackPanel Grid.Row="1" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Stretch" >
            <!-- New -->
            <Border Padding="8" Margin="0,0,0,8" CornerRadius="6" Background="#434343">
                <DockPanel>
                    <TextBlock Text="New" FontWeight="SemiBold" DockPanel.Dock="Left" Foreground="RoyalBlue"/>
                    <TextBlock Text="{Binding NewCount}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right" DockPanel.Dock="Right" Foreground="RoyalBlue"/>
                </DockPanel>
            </Border>

            <!-- Learning -->
            <Border Padding="8" Margin="0,0,0,8" CornerRadius="6" Background="#434343">
                <DockPanel>
                    <TextBlock Text="Learning" FontWeight="SemiBold" DockPanel.Dock="Left" Foreground="#D32F2F"/>
                    <TextBlock Text="{Binding LearningCount}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right" DockPanel.Dock="Right" Foreground="#D32F2F"/>
                </DockPanel>
            </Border>

            <!-- Review -->
            <Border Padding="8" Margin="0,0,0,8" CornerRadius="6" Background="#434343">
                <DockPanel>
                    <TextBlock Text="Review" FontWeight="SemiBold" DockPanel.Dock="Left" Foreground="#388E3C"/>
                    <TextBlock Text="{Binding ReviewCount}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right" DockPanel.Dock="Right" Foreground="#388E3C"/>
                </DockPanel>
            </Border>
        </StackPanel>

        <!-- Nút Study Now -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
            <Button Content="Study Now"
                    Width="120"
                    Height="36"
                    FontSize="14"
                    FontWeight="Bold"
                    Background="#FF367FF4"
                    Foreground="White"
                    Cursor="Hand"
                    Command="{Binding StudyNowCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>
    </Grid>
</Window>