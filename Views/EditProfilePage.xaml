<Page x:Class="EasyFlips.Views.EditProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:EasyFlips.ViewModels"
      mc:Ignorable="d"
      Title="Edit Profile"
      d:DesignHeight="600" d:DesignWidth="800">

    <Page.Resources>
        <SolidColorBrush x:Key="BgDark" Color="#181A20"/>
        <SolidColorBrush x:Key="SurfaceDark" Color="#1F222A"/>
        <SolidColorBrush x:Key="CardBg" Color="#2B2E38"/>
        <SolidColorBrush x:Key="PrimaryPurple" Color="#6C5DD3"/>
        <SolidColorBrush x:Key="TextGrey" Color="#9E9EA7"/>
        <SolidColorBrush x:Key="InputBg" Color="#2B2E38"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border Background="{StaticResource InputBg}" CornerRadius="8" BorderBrush="#333" BorderThickness="1"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryPurple}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bdr" Property="Background" Value="#444"/>
                                <Setter Property="Foreground" Value="#888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="#2B2E38"/>
            <Setter Property="Foreground" Value="{StaticResource TextGrey}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="10" BorderBrush="#444" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="#353945"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="Transparent">

        <Border Width="400" Height="550" 
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="{StaticResource SurfaceDark}" 
                CornerRadius="15" BorderBrush="#333" BorderThickness="1">

            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="30" Opacity="0.6" ShadowDepth="10"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="20,20,20,10">
                    <TextBlock Text="Edit Profile" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                </Grid>

                <StackPanel Grid.Row="1" Margin="30,10,30,0">

                    <Grid HorizontalAlignment="Center" Margin="0,0,0,30">
                        <Border Width="110" Height="110" CornerRadius="55" BorderBrush="{StaticResource PrimaryPurple}" BorderThickness="2">
                            <Border.Effect>
                                <DropShadowEffect Color="#6C5DD3" BlurRadius="15" Opacity="0.3"/>
                            </Border.Effect>
                            <Border.Background>
                                <ImageBrush ImageSource="{Binding AvatarURL, FallbackValue='pack://application:,,,/Images/default_user.png', TargetNullValue='pack://application:,,,/Images/default_user.png'}" 
                                            Stretch="UniformToFill"/>
                            </Border.Background>
                        </Border>

                        <Button Command="{Binding ChangeAvatarCommand}"
                                Width="32" Height="32"
                                HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                Background="{StaticResource PrimaryPurple}" BorderThickness="0" Cursor="Hand"
                                ToolTip="Upload new avatar">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="16" BorderBrush="{StaticResource SurfaceDark}" BorderThickness="3">
                                        <TextBlock Text="📷" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>

                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="User Name" Foreground="{StaticResource TextGrey}" Margin="0,0,0,8" FontSize="13" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" MaxLength="30" Height="45"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,30">
                        <TextBlock Text="Email Address" Foreground="{StaticResource TextGrey}" Margin="0,0,0,8" FontSize="13" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding Email}" IsReadOnly="True" Height="45" Foreground="#666" ToolTip="Email cannot be changed."/>
                    </StackPanel>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Content="CANCEL" Height="45"
                                Command="{Binding CancelCommand}" 
                                Style="{StaticResource SecondaryBtnStyle}"/>

                        <Button Grid.Column="2" Content="SAVE CHANGES" Height="45"
                                Command="{Binding SaveProfileCommand}" 
                                IsEnabled="{Binding CanSave}"
                                Style="{StaticResource PrimaryBtnStyle}"/>
                    </Grid>
                </StackPanel>

                <ProgressBar Grid.Row="2" IsIndeterminate="True" Height="4" VerticalAlignment="Bottom" 
                             Background="Transparent" BorderThickness="0" Foreground="{StaticResource PrimaryPurple}"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>
            </Grid>
        </Border>
    </Grid>
</Page>