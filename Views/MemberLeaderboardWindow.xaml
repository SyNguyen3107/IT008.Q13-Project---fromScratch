<Window x:Class="EasyFlips.Views.MemberLeaderboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cv="clr-namespace:EasyFlips.Converters"
        Title="Leaderboard" Height="850" Width="1100"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <cv:NullToCollapsedConverter x:Key="NullToCollapsed"/>
        <cv:ImagePathConverter x:Key="ImgConv"/>
        <SolidColorBrush x:Key="Color.Primary" Color="#6C5DD3"/>
        <SolidColorBrush x:Key="Color.Accent" Color="#FFCE73"/>
        <Style x:Key="ModernScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false" />
            <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
            <Setter Property="Width" Value="8" />
            <Setter Property="Margin" Value="5,0,0,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true">
                            <Border CornerRadius="4" Background="#0AFFFFFF" />
                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsEnabled}">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border x:Name="thumbBorder" 
                                                    Background="#33FFFFFF" 
                                                    CornerRadius="4" />
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="thumbBorder" Property="Background" Value="#66FFFFFF" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollViewer" TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" 
                                                CanContentScroll="{TemplateBinding CanContentScroll}" />
                            <ScrollBar x:Name="PART_VerticalScrollBar" 
                                   HorizontalAlignment="Right"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                   Value="{TemplateBinding VerticalOffset}"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Style="{StaticResource ModernScrollBar}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="40" BorderThickness="1.5" BorderBrush="#33FFFFFF" ClipToBounds="True" Background="#1B1D21">
        <Grid>
            <Grid IsHitTestVisible="False">
                <Ellipse Width="700" Height="700" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-250,-250,0,0" Opacity="0.15">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="#6C5DD3" Offset="0"/>
                            <GradientStop Color="Transparent" Offset="1"/>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
                <Ellipse Width="700" Height="700" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-250,-250" Opacity="0.1">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="#FFCE73" Offset="0"/>
                            <GradientStop Color="Transparent" Offset="1"/>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
            </Grid>

            <Grid Margin="50,40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="450"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="C H A M P I O N S" FontSize="13" FontWeight="Bold" Foreground="#66FFFFFF" HorizontalAlignment="Center" FontFamily="Inter V"/>
                    <TextBlock Text="LEADERBOARD" FontSize="48" FontWeight="Black" Foreground="White" HorizontalAlignment="Center" FontFamily="Poppins"/>
                </StackPanel>

                <Grid Grid.Row="1" VerticalAlignment="Bottom">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="1.3*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" VerticalAlignment="Bottom" Margin="10,0">
                        <StackPanel>
                            <Ellipse Width="110" Height="110" Stroke="#A0A0A0" StrokeThickness="3" HorizontalAlignment="Center">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding SecondPlace.AvatarUrl, Converter={StaticResource ImgConv}}" Stretch="UniformToFill"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Text="{Binding SecondPlace.Name}" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10"/>
                            <Border Height="140" CornerRadius="25,25,10,10" Background="#15FFFFFF" BorderThickness="1,1,1,0" BorderBrush="#20FFFFFF">
                                <TextBlock Text="2" FontSize="70" FontWeight="Black" Foreground="#0DFFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Column="2" VerticalAlignment="Bottom" Margin="10,0">
                        <StackPanel>
                            <Ellipse Width="100" Height="100" Stroke="#CD7F32" StrokeThickness="3" HorizontalAlignment="Center">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding ThirdPlace.AvatarUrl, Converter={StaticResource ImgConv}}" Stretch="UniformToFill"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Text="{Binding ThirdPlace.Name}" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10"/>
                            <Border Height="110" CornerRadius="20,20,10,10" Background="#10FFFFFF" BorderThickness="1,1,1,0" BorderBrush="#15FFFFFF">
                                <TextBlock Text="3" FontSize="60" FontWeight="Black" Foreground="#08FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Column="1" VerticalAlignment="Bottom" Panel.ZIndex="10">
                        <StackPanel>
                            <Path Data="M 0,10 L 5,0 L 10,10 L 15,0 L 20,10 L 20,15 L 0,15 Z" Fill="#FFCE73" Width="50" Height="40" Stretch="Uniform" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <Ellipse Width="160" Height="160" Stroke="#FFCE73" StrokeThickness="6" HorizontalAlignment="Center">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding FirstPlace.AvatarUrl, Converter={StaticResource ImgConv}}" Stretch="UniformToFill"/>
                                </Ellipse.Fill>
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="#FFCE73" BlurRadius="30" Opacity="0.4" ShadowDepth="0"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            <TextBlock Text="{Binding FirstPlace.Name}" Foreground="White" FontSize="32" FontWeight="Black" HorizontalAlignment="Center" Margin="0,12"/>
                            <Border Height="177" CornerRadius="35,35,15,15" BorderThickness="1,1,1,0" BorderBrush="#44FFFFFF">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#6C5DD3" Offset="0"/>
                                        <GradientStop Color="#4A3DBB" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Grid>
                                    <TextBlock Text="1" FontSize="120" FontWeight="Black" Foreground="#20FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,20"/>
                                    <TextBlock Text="{Binding FirstPlace.Score}" Foreground="#FFCE73" FontSize="45" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,30"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Grid>

                <Border Grid.Row="2" Background="#0DFFFFFF" CornerRadius="30" Margin="0,30,0,0" Padding="25">
                    <ScrollViewer VerticalScrollBarVisibility="Hidden">
                        <ItemsControl ItemsSource="{Binding RestOfPlayers}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#0FFFFFFF" CornerRadius="20" Margin="0,0,0,10" Padding="20,15" BorderThickness="1" BorderBrush="#10FFFFFF">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Rank}" FontSize="22" FontWeight="Black" Foreground="#22FFFFFF" VerticalAlignment="Center"/>
                                            <Ellipse Grid.Column="1" Width="55" Height="55" Margin="15,0">
                                                <Ellipse.Fill>
                                                    <ImageBrush ImageSource="{Binding AvatarUrl, Converter={StaticResource ImgConv}}" Stretch="UniformToFill"/>
                                                </Ellipse.Fill>
                                            </Ellipse>
                                            <TextBlock Grid.Column="2" Text="{Binding Name}" Foreground="White" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
                                            <Border Grid.Column="3" Background="#20FFFFFF" CornerRadius="12" Padding="20,8">
                                                <TextBlock Text="{Binding Score}" Foreground="#FFCE73" FontSize="24" FontWeight="Black" VerticalAlignment="Center"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                    <Button Content="SAVE IMAGE" Click="OnSaveImageClick" Width="180" Height="55" Margin="12,0" Background="#2A2E35" Foreground="White" FontWeight="Bold" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="16" BorderThickness="1" BorderBrush="#33FFFFFF">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="b" Property="Background" Value="#3A3E45"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    
                    <Button Content="LEAVE ROOM" Command="{Binding  LeaveRoomCommand}" Width="180" Height="55" Margin="12,0" Background="#C0392B" Foreground="White" FontWeight="Bold" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="16">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#C0392B" BlurRadius="15" Opacity="0.3" ShadowDepth="0"/>
                                    </Border.Effect>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="b" Property="Background" Value="#E74C3C"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>

            <Button Command="{Binding LeaveRoomCommand}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="30" Content="✕" Foreground="#44FFFFFF" FontSize="20" Width="40" Height="40" Background="Transparent" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="Transparent" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#20FFFFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>