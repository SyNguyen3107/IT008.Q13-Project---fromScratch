<Window x:Class="EasyFlips.Views.ErrorDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="L?i ??ng nh?p" 
        Height="280" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White"
        WindowStyle="None"
        AllowsTransparency="True">
    <Border BorderBrush="#E0E0E0" BorderThickness="1" Background="White" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Content Area -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="25">
                <!-- Error Icon -->
                <Image x:Name="ErrorImage" 
                       Source="pack://application:,,,/Resources/error.png" 
                       Width="60" 
                       Height="60" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,15"/>

                <!-- Error Message -->
                <TextBlock x:Name="ErrorMessageText"
                           FontSize="15"
                           FontWeight="Bold"
                           Foreground="#37474F"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="440"
                           LineHeight="22"/>
            </StackPanel>

            <!-- Close Button -->
            <Button Grid.Row="1"
                    x:Name="CloseButton"
                    Content="Close"
                    Click="CloseButton_Click"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Foreground="White"
                    Background="#4CAF50"
                    Height="38"
                    Width="140"
                    Margin="0,0,0,20"
                    HorizontalAlignment="Center"
                    Cursor="Hand"
                    BorderThickness="0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#4CAF50"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="19"
                                            BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
