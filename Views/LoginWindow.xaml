<Window x:Class="EasyFlips.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasyFlips.Views"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background ="#1E1E1E">
    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="280">
            <!--Picture-->
            <Image Source="pack://application:,,,/Resources/lock.png" Width="100" Height="100" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <!-- Welcome -->
            <TextBlock Text="Welcome Back!" FontSize="20" Foreground="White" HorizontalAlignment="Center"/>
            <TextBlock Text="Log in to your existing account" FontSize="14" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,20"/>

            <!-- Email -->
            <TextBox x:Name="txtEmail" 
                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="14" GotFocus="Email_GotFocus" LostFocus="Email_LostFocus" Margin="0,5" Height="35" Background="#40444B" Foreground="White"/>

            <!-- Password -->
            <PasswordBox x:Name="txtPassword" Tag="Enter Password" FontSize ="14" GotFocus="Password_GotFocus" LostFocus="Password_LostFocus" Margin="0,5" Height="35" Background="#40444B" Foreground="White"/>

            <!-- Login Button -->
            <Button x:Name="btnLogin"
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding ElementName=txtPassword}"
                    Content="Login" FontSize="14" Margin="60,15" Height="35" Background="#3498DB" Foreground="White"/>
            
            <!-- Register Link -->
            <TextBlock HorizontalAlignment="Center" Margin="0,10,0,0">
                <Hyperlink FontSize="14" Foreground="#00AFF0">Register</Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
