<Window x:Class="IT008.Q13_Project___fromScratch.MainAnkiWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IT008.Q13_Project___fromScratch"
        mc:Ignorable="d"
                Title="Anki" Height="800" Width="1200"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#2c2c2c"
        Loaded="MainAnkiWindowLoaded">
    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Chia layout 4 hàng -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu trên cùng -->
        <Menu Grid.Row="0" Background="#2c2c2c" Foreground="White" FontSize="14" BorderThickness="0">
            <MenuItem Header="_File">
                <MenuItem Header="_Switch Profile" Cursor="Hand" InputGestureText="Ctrl+Shift+P" Foreground="Black"/>
                <Separator/>
                <MenuItem Header="_Import" Cursor="Hand" InputGestureText="Ctrl+Shift+I" Foreground="Black"/>
                <MenuItem Header="_Export" Cursor="Hand" InputGestureText="Ctrl+E" Foreground="Black"/>
                <Separator/>
                <MenuItem Header="Create _Backup" Cursor="Hand" Foreground="Black"/>
                <MenuItem Header="_Revert to Backup" Cursor="Hand" Foreground="Black"/>
                <Separator/>
                <MenuItem Header="E_xit" Cursor="Hand" InputGestureText="Ctrl+Q" Foreground="Black"/>
            </MenuItem>

            <MenuItem Header="_Edit" Foreground="White">
                <MenuItem Header="Undo" Cursor="Hand" InputGestureText="Ctrl+Z" Foreground="Gray"/>
            </MenuItem>

            <MenuItem Header="_View" Foreground="White">
                <MenuItem Header="Toggle _Full Screen" Cursor="Hand" InputGestureText="F11" Foreground="Black"/>
                <Separator/>
                <MenuItem Header="Zoom _In" Cursor="Hand" Foreground="Black"/>
                <MenuItem Header="Zoom _Out" Cursor="Hand" Foreground="Black"/>
                <MenuItem Header="_Reset Zoom" Cursor="Hand" InputGestureText="Ctrl+0" Foreground="Black"/>
            </MenuItem>

            <MenuItem Header="_Tools" Foreground="White">
                <MenuItem Header="Study Deck..." Cursor="Hand" InputGestureText="/" Foreground="Black"/>
                <MenuItem Header="Create Filtered Deck..." Cursor="Hand" InputGestureText="F" Foreground="Black"/>
                <Separator/>
                <MenuItem Header="_Check Database" Cursor="Hand" Foreground="Black"/>
                <MenuItem Header="Check _Media" Cursor="Hand" Foreground="Black"/>
                <MenuItem Header="Empty Cards..." Cursor="Hand" Foreground="Black"/>
                <Separator/>
                <MenuItem Header="Add-ons" Cursor="Hand" InputGestureText="Ctrl+Shift+A" Foreground="Black"/>
                <MenuItem Header="Manage Note Types" Cursor="Hand" InputGestureText="Ctrl+Shift+N" Foreground="Black"/>
                <MenuItem Header="Upgrade/Downgrade" Cursor="Hand" Foreground="Black"/>
                <MenuItem Header="_Preferences" Cursor="Hand" InputGestureText="Ctrl+P" Foreground="Black"/>
            </MenuItem>

            <MenuItem Header="_Help" Foreground="White">
                <MenuItem Header="_Guide" Cursor="Hand" InputGestureText="Ctrl+F1" Foreground="Black"/>
                <Separator/>
                <MenuItem Header="_Support Anki" Cursor="Hand" Foreground="Black"/>
                <MenuItem Header="About Anki" Cursor="Hand" Foreground="Black"/>
            </MenuItem>
        </Menu>

        <!-- Thanh Deck/Add/Browse/Stats/Sync -->
        <Border Grid.Row="1"
                Background="#434343"
                CornerRadius="15"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                BorderBrush="#444"
                BorderThickness="1"
                Padding="5,0" Margin="443,0,0,0" Height="34">

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Content="Decks" Style="{StaticResource NavButtonStyle}" Margin="10,0"/>
                <Button Content="Add" Style="{StaticResource NavButtonStyle}" Margin="10,0"/>
                <Button Content="Browse" Style="{StaticResource NavButtonStyle}" Margin="10,0"/>
                <Button Content="Stats" Style="{StaticResource NavButtonStyle}" Margin="10,0"/>
                <Button Content="Sync" Style="{StaticResource NavButtonStyle}" Margin="10,0"/>
            </StackPanel>
        </Border>
        <!-- Bảng hiển thị Deck -->
        <Border Grid.Row="2"
                Height="Auto"
                Width="800"
                Background="#363636"
                CornerRadius="15"
                Margin="30">
            <ListView ItemsSource="{Binding Decks}"  Background="Transparent"
          BorderThickness="0" Margin="0, 0, 0, 0" Grid.RowSpan="2">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Deck"
                            Width="580">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" 
                                   VerticalAlignment="Center" 
                                   Padding="5"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="New"
                            Width="50">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding NewCount}" 
                                   TextAlignment="Right" 
                                   Foreground="RoyalBlue"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"
                                   Padding="5"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Learn"
                            Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding LearnCount}" 
                                   TextAlignment="Right" 
                                   Foreground="Red"
                                   VerticalAlignment="Center"
                                   Padding="5"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Due"
                            Width="50">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DueCount}" 
                                   TextAlignment="Right" 
                                   Foreground="ForestGreen"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"
                                   Padding="5"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header=""
                            Width="40">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="⚙" 
                                Command="{Binding OptionsCommand}"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

        </Border>

        <!-- Thanh nút dưới -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,20">
            <Border Background="#434343" CornerRadius="15" Padding="5,2" Margin="8,0">
                <Button Content="Get Shared" Command ="{Binding GetSharedCommand}" Style="{StaticResource NavButtonStyle}"/>
            </Border>
            <Border Background="#434343" CornerRadius="15" Padding="5,2" Margin="8,0">
                <Button Content="Create Deck" Command="{Binding CreateDeckCommand }" Style="{StaticResource NavButtonStyle}" Click="CreateDeckWindowWindowOpen" />
            </Border>
            <Border Background="#434343" CornerRadius="15" Padding="5,2" Margin="8,0">
                <Button Content="Import File" Command ="{Binding ImportFileCommand}" Style="{StaticResource NavButtonStyle}" Click="ImportFileWindowOpen"/>
            </Border>
        </StackPanel>
    </Grid>
</Window>
