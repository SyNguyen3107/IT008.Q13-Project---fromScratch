<Window x:Class="EasyFlips.Views.HostLobbyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:EasyFlips.ViewModels"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:converters="clr-namespace:EasyFlips.Converters"
        mc:Ignorable="d"
        Title="EasyFlips - Host Lobby" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        WindowState="Maximized">


    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding WindowClosingCommand}" 
                                   PassEventArgsToCommand="True"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <LinearGradientBrush x:Key="MainBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#1B1D21" Offset="0.0"/>
            <GradientStop Color="#2C2F33" Offset="1.0"/>
        </LinearGradientBrush>

        <Style x:Key="GlassCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1A1C1F"/>
            <Setter Property="BorderBrush" Value="#33FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" Color="Black" Opacity="0.3" ShadowDepth="3"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#22262A"/>
                    <Setter Property="BorderBrush">
                        <Setter.Value>
                            <SolidColorBrush Color="{StaticResource Color.Primary}"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect BlurRadius="25" Color="{StaticResource Color.Primary}" Opacity="0.4" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="15" Background="{TemplateBinding Background}" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="FontFamily" Value="Poppins"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="#20FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource MainBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="#15181C" Padding="35,25" BorderBrush="#33FFFFFF" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Room Code Display -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Background="{StaticResource Brush.Primary}" Padding="10,5" CornerRadius="8" Margin="0,0,15,0"
                            VerticalAlignment="Center">
                        <TextBlock Text="ROOM CODE" Foreground="White" FontSize="12" FontWeight="Bold" FontFamily="Inter V"/>
                    </Border>

                    <Border Background="#0F1115" CornerRadius="18" BorderBrush="#33FFFFFF" BorderThickness="1" 
                            Padding="35,20" VerticalAlignment="Center">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="20" Color="{StaticResource Color.Primary}" Opacity="0.3" ShadowDepth="0"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding RoomId, TargetNullValue='LOADING'}"
                                       FontSize="52" FontWeight="Black" Foreground="White" FontFamily="Poppins"
                                       VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="{StaticResource Color.Primary}" BlurRadius="20" Opacity="0.6" ShadowDepth="0"/>
                                </TextBlock.Effect>
                            </TextBlock>

                            <Button Grid.Column="1" Command="{Binding CopyRoomCodeCommand}"
                                    Width="36" Height="36" Margin="20,0,0,0" VerticalAlignment="Center"
                                    Style="{StaticResource IconButtonStyle}" ToolTip="Copy room code">
                                <Image Source="/Resources/copy.png" Width="20" Height="20" Stretch="Uniform"/>
                            </Button>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- Sound Toggle Button -->
                <Button Grid.Column="1" VerticalAlignment="Center" Command="{Binding ToggleMuteCommand}"
                        Width="44" Height="44" Style="{StaticResource IconButtonStyle}">
                    <Image Width="24" Height="24" Stretch="Uniform">
                        <Image.Style>
                            <Style TargetType="Image">
                                <Setter Property="Source" Value="/Resources/volume.png"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMuted}" Value="True">
                                        <Setter Property="Source" Value="/Resources/mute.png"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>
                </Button>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="35,25,35,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Players Area -->
            <Border Grid.Column="0" Background="#15181C" CornerRadius="25" Padding="30" Margin="0,0,20,0"
                    BorderBrush="#33FFFFFF" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Status Bar -->
                    <Border Background="#1A1C1F" CornerRadius="15" Padding="20,15" Margin="0,0,0,25">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="12" Height="12" Fill="{StaticResource Brush.Primary}" Margin="0,0,10,0"/>
                                    <TextBlock Text="ONLINE: " Foreground="#94A3B8" FontWeight="SemiBold" 
                                               VerticalAlignment="Center" FontSize="14" FontFamily="Inter V"/>
                                    <TextBlock Text="{Binding Players.Count}" Foreground="White" FontWeight="Bold" 
                                               Margin="5,0,0,0" VerticalAlignment="Center" FontSize="14" FontFamily="Poppins"/>
                                    <TextBlock Text="{Binding MaxPlayersString}" Foreground="#66FFFFFF" 
                                               VerticalAlignment="Center" FontSize="14" FontFamily="Inter V"/>
                                </StackPanel>

                                <TextBlock Text="{Binding AutoStartStatus}" Foreground="{StaticResource Brush.Accent2}" 
                                           FontWeight="Bold" FontSize="13" HorizontalAlignment="Right" 
                                           VerticalAlignment="Center" FontFamily="Inter V"/>
                            </Grid>

                            <ProgressBar Value="{Binding AutoStartSeconds}" Maximum="{Binding TotalWaitTime}"
                                         Height="6" BorderThickness="0" Background="#0F1115" 
                                         Foreground="{StaticResource Brush.Accent2}">
                                <ProgressBar.Resources>
                                    <Style TargetType="ProgressBar">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ProgressBar">
                                                    <Grid x:Name="TemplateRoot">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="3"/>
                                                        <Border x:Name="PART_Track"/>
                                                        <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}" 
                                                                CornerRadius="3" HorizontalAlignment="Left">
                                                            <Border.Effect>
                                                                <DropShadowEffect Color="{StaticResource Color.Accent2}" 
                                                                                  BlurRadius="10" Opacity="0.6" ShadowDepth="0"/>
                                                            </Border.Effect>
                                                        </Border>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ProgressBar.Resources>
                            </ProgressBar>
                        </StackPanel>
                    </Border>

                    <!-- Players Grid -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding Players}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel HorizontalAlignment="Center" Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="150" Height="190" Margin="12" Style="{StaticResource GlassCardStyle}"
                                            RenderTransformOrigin="0.5,0.5" Padding="15">
                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                                        </Border.RenderTransform>
                                        <Border.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                                         From="0.3" To="1" Duration="0:0:0.6">
                                                            <DoubleAnimation.EasingFunction>
                                                                <ElasticEase EasingMode="EaseOut" Oscillations="1" Springiness="5"/>
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                                         From="0.3" To="1" Duration="0:0:0.6">
                                                            <DoubleAnimation.EasingFunction>
                                                                <ElasticEase EasingMode="EaseOut" Oscillations="1" Springiness="5"/>
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Border.Triggers>

                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <Grid Margin="0,0,0,18">
                                                <Ellipse Width="92" Height="92" Stroke="{StaticResource Brush.Primary}" 
                                                         StrokeThickness="3" Opacity="0.5"/>
                                                <Ellipse Width="86" Height="86">
                                                    <Ellipse.Fill>
                                                        <ImageBrush ImageSource="{Binding AvatarUrl, TargetNullValue='https://i.imgur.com/KkrzQ83.png'}" 
                                                                    Stretch="UniformToFill"/>
                                                    </Ellipse.Fill>
                                                </Ellipse>
                                            </Grid>

                                            <TextBlock Text="{Binding Name}" Foreground="White" 
                                                       HorizontalAlignment="Center" TextAlignment="Center"
                                                       TextTrimming="CharacterEllipsis" MaxWidth="130"
                                                       FontSize="15" FontWeight="SemiBold" FontFamily="Poppins"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Settings Panel -->
            <Border Grid.Column="1" Background="#15181C" CornerRadius="25" Padding="28" 
                    BorderBrush="#33FFFFFF" BorderThickness="1">
                <StackPanel>
                    <Border Background="{StaticResource Brush.Secondary}" Padding="10,5" CornerRadius="8" 
                            HorizontalAlignment="Left" Margin="0,0,0,20">
                        <TextBlock Text="SETTINGS" Foreground="White" FontSize="11" FontWeight="Bold" FontFamily="Inter V"/>
                    </Border>

                    <TextBlock Text="Room Settings" FontSize="22" FontWeight="Bold" Margin="0,0,0,25" 
                               Foreground="White" FontFamily="Poppins"/>

                    <!-- Deck Selection -->
                    <Border Background="#1A1C1F" CornerRadius="15" Padding="18" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="Deck" FontWeight="SemiBold" Margin="0,0,0,10" 
                                       Foreground="#94A3B8" FontSize="13" FontFamily="Inter V"/>
                            <ComboBox ItemsSource="{Binding AvailableDecks}" 
                                      SelectedItem="{Binding SelectedDeck}"
                                      DisplayMemberPath="Name"
                                      Height="42" Padding="12" Background="#0F1115" Foreground="White"
                                      BorderBrush="#33FFFFFF" BorderThickness="1" FontFamily="Inter V" FontSize="14">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Background" Value="#0F1115"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="Padding" Value="12,8"/>
                                        <Setter Property="FontFamily" Value="Inter V"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ComboBoxItem">
                                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" 
                                                            Padding="{TemplateBinding Padding}" CornerRadius="6" Margin="2">
                                                        <ContentPresenter/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsHighlighted" Value="True">
                                                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.Primary}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.Secondary}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                                <ComboBox.Resources>
                                    <Style TargetType="ComboBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ComboBox">
                                                    <Grid>
                                                        <ToggleButton x:Name="ToggleButton" Grid.Column="2"
                                                                      Focusable="false"
                                                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                                      ClickMode="Press"
                                                                      Background="{TemplateBinding Background}"
                                                                      BorderBrush="{TemplateBinding BorderBrush}"
                                                                      BorderThickness="{TemplateBinding BorderThickness}">
                                                            <ToggleButton.Template>
                                                                <ControlTemplate TargetType="ToggleButton">
                                                                    <Border Background="{TemplateBinding Background}" 
                                                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                                            CornerRadius="10">
                                                                        <Grid>
                                                                            <ContentPresenter Margin="12,0"/>
                                                                            <Path x:Name="Arrow" HorizontalAlignment="Right" VerticalAlignment="Center"
                                                                                  Margin="0,0,12,0" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#66FFFFFF"/>
                                                                        </Grid>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </ToggleButton.Template>
                                                        </ToggleButton>
                                                        <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                                          Margin="12,0,30,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                        <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                                            <Border Background="#0F1115" BorderBrush="{StaticResource Brush.Primary}" BorderThickness="1.5" 
                                                                    CornerRadius="10" Margin="0,5,0,0" Padding="5"
                                                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                                                <Border.Effect>
                                                                    <DropShadowEffect Color="{StaticResource Color.Primary}" BlurRadius="15" Opacity="0.4" ShadowDepth="0"/>
                                                                </Border.Effect>
                                                                <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                                                                    <StackPanel IsItemsHost="True"/>
                                                                </ScrollViewer>
                                                            </Border>
                                                        </Popup>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ComboBox.Resources>
                            </ComboBox>
                        </StackPanel>
                    </Border>

                    <!-- Time Per Round -->
                    <Border Background="#1A1C1F" CornerRadius="15" Padding="18" Margin="0,0,0,15">
                        <Grid>
                            <TextBlock Text="Time Per Round" Foreground="#94A3B8" FontSize="13" 
                                       VerticalAlignment="Center" FontFamily="Inter V"/>
                            <Border Background="{StaticResource Brush.Secondary}" CornerRadius="8" Padding="10,5" 
                                    HorizontalAlignment="Right">
                                <TextBlock Text="{Binding TimePerRound, StringFormat='{}{0} sec'}" 
                                           Foreground="White" FontWeight="Bold" FontSize="13" FontFamily="Poppins"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Max Players -->
                    <Border Background="#1A1C1F" CornerRadius="15" Padding="18" Margin="0,0,0,25">
                        <Grid>
                            <TextBlock Text="Max Players" Foreground="#94A3B8" FontSize="13" 
                                       VerticalAlignment="Center" FontFamily="Inter V"/>
                            <Border Background="{StaticResource Brush.Primary}" CornerRadius="8" Padding="10,5" 
                                    HorizontalAlignment="Right">
                                <TextBlock Text="{Binding MaxPlayers}" Foreground="White" FontWeight="Bold" 
                                           FontSize="13" FontFamily="Poppins"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Change Settings Button -->
                    <Button Command="{Binding OpenSettingsCommand}"
                            Style="{StaticResource ModernButtonStyle}"
                            Content="Change Settings"
                            Background="#2C2F33" Foreground="White" BorderThickness="0"
                            Height="45" Margin="0,0,0,0" FontSize="14" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Footer Section -->
        <Border Grid.Row="2" Background="#15181C" Padding="35,20" BorderBrush="#33FFFFFF" BorderThickness="0,1,0,0">
            <Grid>
                <!-- Dissolve Room Button -->
                <Button Command="{Binding CloseRoomCommand}" 
                        Content="Dissolve Room"
                        HorizontalAlignment="Left"
                        Style="{StaticResource ModernButtonStyle}"
                        Background="#2C2F33" Foreground="#FFA2C0" BorderThickness="0"
                        Width="170" Height="50" FontSize="14" FontWeight="SemiBold"/>

                <!-- Start Game Button -->
                <Button Command="{Binding StartGameCommand}" 
                        Content="START GAME"
                        HorizontalAlignment="Right"
                        Style="{StaticResource ModernButtonStyle}"
                        Background="{StaticResource Brush.Primary}" Foreground="White" BorderThickness="0"
                        Width="220" Height="50" FontSize="16" FontWeight="Bold">
                    <Button.Effect>
                        <DropShadowEffect Color="{StaticResource Color.Primary}" BlurRadius="25" Opacity="0.5" ShadowDepth="0"/>
                    </Button.Effect>
                </Button>
            </Grid>
        </Border>

        <!-- Copy Toast Notification -->
        <Border x:Name="CopyToast"
                Background="{StaticResource Brush.Primary}"
                CornerRadius="15"
                Padding="20,12"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Center"
                Margin="0,0,0,80"
                Opacity="0"
                IsHitTestVisible="False">
            <Border.Effect>
                <DropShadowEffect Color="{StaticResource Color.Primary}" BlurRadius="20" Opacity="0.6" ShadowDepth="0"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="✓" Foreground="White" Margin="0,0,10,0" FontSize="16" FontWeight="Bold"/>
                <TextBlock Text="Room code copied!" Foreground="White" FontWeight="SemiBold" FontFamily="Inter V"/>
            </StackPanel>

            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ShowCopyMessage}" Value="True">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         To="1" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                         From="20" To="0" Duration="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <CircleEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>

                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         To="0" Duration="0:0:0.5"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Border.RenderTransform>
                <TranslateTransform Y="20"/>
            </Border.RenderTransform>
        </Border>
    </Grid>
</Window>
