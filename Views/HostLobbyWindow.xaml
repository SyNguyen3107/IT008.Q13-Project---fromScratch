<Window x:Class="EasyFlips.Views.HostLobbyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:EasyFlips.ViewModels"
        xmlns:converters="clr-namespace:EasyFlips.Converters"
        mc:Ignorable="d"
        Title="EasyFlips - Host Lobby" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        WindowState="Maximized">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <LinearGradientBrush x:Key="MainBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#141E30" Offset="0.0"/>
            <GradientStop Color="#243B55" Offset="1.0"/>
        </LinearGradientBrush>

        <Style x:Key="GlassCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="BorderBrush" Value="#30FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" Color="Black" Opacity="0.3" ShadowDepth="4"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#25FFFFFF"/>
                    <Setter Property="BorderBrush" Value="#4CD137"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect BlurRadius="20" Color="#4CD137" Opacity="0.6" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="25" Background="{TemplateBinding Background}" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#BDC3C7"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="#20FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource MainBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border  Grid.Row="0" Background="{StaticResource MainBackground}" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="ROOM CODE" Foreground="#7F8FA6" FontSize="14" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,5" VerticalAlignment="Center"/>
                    <Border Margin="15, 0, 0 ,0" Background="#20000000" CornerRadius="20" BorderBrush="#20FFFFFF" BorderThickness="1" Padding="60,15">
                        <TextBlock Text="{Binding RoomId, TargetNullValue='LOADING'}" 
                               FontSize="56" FontWeight="Black" Foreground="White"
                               HorizontalAlignment="Center">    
                            <TextBlock.Effect>
                                <DropShadowEffect Color="White" BlurRadius="15" Opacity="0.6" ShadowDepth="0"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </Border>

                    <Button Command="{Binding CopyRoomCodeCommand}" Margin="15,0,0,0" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Sao chép mã">
                        <Image Source="/Resources/copy.png" Width="20" Height="20"/>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button VerticalAlignment="Center" Command="{Binding ToggleMuteCommand}"
                            Width="40" Height="40"
                            HorizontalAlignment="Left"
                            Margin="15"
                            Style="{StaticResource IconButtonStyle}">
                        <Image Width="24" Height="24" Stretch="Uniform">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Setter Property="Source" Value="/Resources/volume.png"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMuted}" Value="True">
                                            <Setter Property="Source" Value="/Resources/mute.png"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource MainBackground}" CornerRadius="10" Padding="15" Margin="0,0,20,0" >
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="10" Height="10" Fill="#4CD137" Margin="0,0,8,0"/>
                        <TextBlock Text="ONLINE: " Foreground="#BDC3C7" FontWeight="Bold" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Players.Count}" Foreground="White" FontWeight="Bold" Margin="5,0,0,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding MaxPlayersString}" Foreground="#7F8FA6" VerticalAlignment="Center"/>
                        <ProgressBar Grid.Row="1"
                                     Value="{Binding AutoStartSeconds}" Maximum="{Binding TotalWaitTime}"
                                     Width="300" Height="6"
                                     BorderThickness="0" Background="#334155" Foreground="#F59E0B"
                                     Margin="15, 0, 0, 0">
                            <ProgressBar.Resources>
                                <Style TargetType="ProgressBar">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ProgressBar">
                                                <Grid x:Name="TemplateRoot">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="3"/>
                                                    <Border x:Name="PART_Track"/>
                                                    <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}" CornerRadius="3" HorizontalAlignment="Left"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ProgressBar.Resources>
                        </ProgressBar>
                        <TextBlock
                            Width="Auto"
                                   Text="{Binding AutoStartStatus}" 
                                   Foreground="#F59E0B" FontWeight="Bold" FontSize="12"
                                   HorizontalAlignment="Center" Margin="0,0,0,5"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" Margin="20,10" VerticalScrollBarVisibility="Hidden">
                        <ItemsControl ItemsSource="{Binding Players}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel HorizontalAlignment="Center" Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="140" Height="180" Margin="15" Style="{StaticResource GlassCardStyle}"
                                RenderTransformOrigin="0.5,0.5">

                                        <Border.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                                        </Border.RenderTransform>
                                        <Border.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             From="0.3" To="1" Duration="0:0:0.6">
                                                            <DoubleAnimation.EasingFunction>
                                                                <ElasticEase EasingMode="EaseOut" Oscillations="1" Springiness="5"/>
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                             From="0.3" To="1" Duration="0:0:0.6">
                                                            <DoubleAnimation.EasingFunction>
                                                                <ElasticEase EasingMode="EaseOut" Oscillations="1" Springiness="5"/>
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Border.Triggers>

                                        <Grid>
                                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <Grid Margin="0,0,0,15">
                                                    <Ellipse Width="84" Height="84" Stroke="#40FFFFFF" StrokeThickness="4"/>
                                                    <Ellipse Width="80" Height="80">
                                                        <Ellipse.Fill>
                                                            <ImageBrush ImageSource="{Binding AvatarUrl, TargetNullValue='https://i.imgur.com/KkrzQ83.png'}" 
                                                            Stretch="UniformToFill"/>
                                                        </Ellipse.Fill>
                                                    </Ellipse>
                                                </Grid>

                                                <TextBlock Text="{Binding Name}" Foreground="White" 
                                               HorizontalAlignment="Center" TextAlignment="Center"
                                               TextTrimming="CharacterEllipsis" MaxWidth="120"
                                               FontSize="16" FontWeight="Bold"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="{StaticResource MainBackground}" CornerRadius="10" Padding="20">
                <StackPanel>
                    <TextBlock Text="Cài đặt phòng" FontSize="16" FontWeight="Bold" Margin="0,0,0,20" Foreground="White"/>

                    <TextBlock Text="Deck:" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="White"/>
                    <ComboBox ItemsSource="{Binding AvailableDecks}" 
                              SelectedItem="{Binding SelectedDeck}"
                              DisplayMemberPath="Name"
                              Padding="10" Margin="0,0,0,20"/>

                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Thời gian mỗi câu:" Foreground="#666"/>
                        <TextBlock Text="{Binding TimePerRound, StringFormat='{}{0} giây'}" FontWeight="Bold" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid Margin="0,0,0,20">
                        <TextBlock Text="Số người tối đa:" Foreground="#666"/>
                        <TextBlock Text="{Binding MaxPlayers}" FontWeight="Bold" HorizontalAlignment="Right"/>
                    </Grid>

                    <Button Command="{Binding OpenSettingsCommand}"
                            Style="{StaticResource ModernButtonStyle}"
                            Content="Thay đổi cài đặt"
                            Background="White" BorderBrush="#CCC" BorderThickness="1" Foreground="#333"
                            Height="40" Margin="0,0,0,10"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource MainBackground}" Padding="20" BorderBrush="#EEE" BorderThickness="0,1,0,0" >
            <Grid>
                <Button Command="{Binding CloseRoomCommand}" 
                        Content="Giải tán phòng"
                        HorizontalAlignment="Left"
                        Style="{StaticResource ModernButtonStyle}"
                        Background="#FFEBEE" Foreground="#D32F2F" BorderThickness="0"
                        Width="150" Height="45" FontSize="14" FontWeight="SemiBold"/>

                <Button Command="{Binding StartGameCommand}" 
                        Content="BẮT ĐẦU GAME"
                        HorizontalAlignment="Right"
                        Style="{StaticResource ModernButtonStyle}"
                        Background="#4CAF50" Foreground="White" BorderThickness="0"
                        Width="200" Height="45" FontSize="16" FontWeight="Bold"/>
            </Grid>
        </Border>
    </Grid>
</Window>